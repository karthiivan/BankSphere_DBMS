# Environment Configuration for BankSphere (PostgreSQL Version)

# PostgreSQL Database Connection (for Render deployment)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://banksphere_user:password@localhost:5432/banksphere

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Session Secret
SESSION_SECRET=your_session_secret_key_change_this

# BCrypt Rounds
BCRYPT_ROUNDS=12

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Optional: Individual PostgreSQL connection parameters (for local development)
# These are not needed if DATABASE_URL is set
# DB_HOST=localhost
# DB_USER=postgres
# DB_PASSWORD=your_password
# DB_NAME=banksphere
# DB_PORT=5432
